import 'package:flutter/material.dart';

class PostArticleTab extends StatelessWidget {
  const PostArticleTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
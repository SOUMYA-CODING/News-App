import 'package:flutter/material.dart';

class SavedArticlesTab extends StatelessWidget {
  const SavedArticlesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [Text("Hello Saved Articles")],
    );
  }
}
